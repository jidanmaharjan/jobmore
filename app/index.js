import { View, Text } from 'react-native'
import { SafeAreaView } from 'react-native-web'

const Home = () => {
  return (
    <SafeAreaView style={
      {
        flex: 1,
        backgroundColor: 'white'
      }
    }>
      
    </SafeAreaView>
  )
}

export default Home